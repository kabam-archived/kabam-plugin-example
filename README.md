mwc_plugin_example
===================

Example plugin to mwc_core application.
Add routes
  /config
  /time
  /team
  /redis
  /honeypot

Developer Note
==============

Run jshint with:

    grunt jshint
